# ============================================
# 本地开发配置选项
# ============================================

# 选项 1: 使用远程数据库（当前配置）
# 优点: 与生产环境数据一致，无需本地安装数据库
# 缺点: 测试数据会写入生产数据库
DATABASE_URL=postgresql://postgres:Wax135!!!@39.97.193.79:5432/postgres?options=-csearch_path%%3Dtiku

# 选项 2: 使用本地 PostgreSQL（推荐用于开发测试）
# 优点: 测试数据与生产隔离，可以随意测试
# 缺点: 需要本地安装 PostgreSQL
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/paper_db

# 选项 3: 使用本地 Docker PostgreSQL
# 先运行: docker run -d --name local_postgres -p 5433:5432 -e POSTGRES_PASSWORD=postgres postgres:14
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/paper_db

# ============================================

# File Storage - Local paths
UPLOAD_DIR=./storage/uploads
IMAGE_DIR=./storage/images

# File Upload Limits
MAX_FILE_SIZE=10485760

# Server Configuration
HOST=0.0.0.0
PORT=8001

# Environment
ENVIRONMENT=development
